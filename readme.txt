git init 
git config --local (propiedades user.name)
git config --list
git status
git add .
git commit -m"comentario" guarada la vercion

git checkout [nombre archivo]
git checkout [id commit]
 
git diff para ver los cambio en el documento.

gir diff -- [nombre archivo] este es para 

git log    para ver verciones 

git log -p   para ver las verciones y todos los archivos.

git log -p -2 muertra las ultimas 2 verciones



https://github.com/huverandrey/clase.git


git push origin master

git pull

git branch
